package main

type server struct{}

func main() {
	// lis, err := net.Listen("tcp", "localhost:3031")
	// if err != nil {
	// 	log.Fatalf("failed to listen: %v", err)
	// }
	// grpcServer := grpc.NewServer()
	// language.
	// grpcServer.Serve(lis)
}
